# ExApp wrapper (this repo). The capture stack (aiortc/websockets/grpc) comes in
# at step 2, when services/nc-capture moves in from the core repo.
nc-py-api>=0.30.2          # 0.30+ deprecates sync wrappers — async only
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
